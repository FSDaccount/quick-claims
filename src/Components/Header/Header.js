import './WebHeader.css';
import Menu from "./Menu";

const Header = () => {
    return (
        <div className="webHeader">
            <h1>Quick Claims</h1>
            <Menu/>
        </div>
    );
}

export default Header
